### Oxfs Agent Server

```bash
usage: oxag-0.0.1-Linux-x86_64 [-h] [-b BIND] [-p PORT]

optional arguments:
  -h, --help            show this help message and exit
  -b BIND, --bind BIND  eg: 0.0.0.0
  -p PORT, --port PORT  eg: 1234
```
